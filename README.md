# Ambient-Node container (temporary architecture)

```
ambient-node/
├── docker-compose.yml
├── mosquitto/
│   ├── Dockerfile
│   ├── config/
│   │   ├── mosquitto.conf
│   │   └── password_file (생성 예정)
│   ├── data/
│   └── log/
├── db-service/
│   └── (이전 가이드 참조)
└── fan-service/
    └── (기존 파일들)
```
